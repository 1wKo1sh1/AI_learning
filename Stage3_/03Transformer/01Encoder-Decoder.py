"""
特殊标记：0，用于填充或者标记未知单词
<SOS>: 句子起始标识符
<EOS>：句子结束标识符
<PAD>：补全字符
<MASK>：掩盖字符
<SEP>：两个句子之间的分隔符
<UNK>：低频或未出现在词表中的词

一、基本结构
编码器（Encoder）：接收变长的输入序列（如一句话、一段语音），通过循环神经网络（RNN/LSTM/GRU）、卷积网络（CNN）或 Transformer 等模块，
                 逐步提取特征，最终输出一个上下文向量（context vector，也称为 thought vector）。该向量试图压缩整个输入序列的语义信息。
解码器（Decoder）：以编码器输出的上下文向量为初始状态（或条件），自回归地生成目标输出序列。每一步生成一个元素（如单词、音素），并将上一步的
                 输出作为当前步的输入，直到生成结束符（如 <EOS>）

二、关键细节
信息瓶颈：
原始结构中，编码器必须将所有输入信息压缩到一个固定维度的向量中，当序列过长时容易出现信息丢失。注意力机制的引入解决了这一问题。
注意力机制：解码器每一步生成时，可以动态“回看”编码器所有隐藏状态，计算与当前步最相关的输入片段（加权求和）。这使得模型能够聚焦于关键信息，显著提升长序列建模能力。
训练与推理差异：
训练时通常使用Teacher Forcing：将真实的目标序列作为解码器每一步的输入，加速收敛。
推理时使用自回归生成（如贪心搜索、束搜索），每一步的输出作为下一步输入。
"""